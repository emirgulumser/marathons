# Bulky raw Garmin Connect dumps (re-import locally if needed)
DI_CONNECT/
garmin_data/

# Prefer FIT under data/fits/; keep loose root zips and legacy GPX out
*.zip
!data/fits/
!data/fits/**/
!data/fits/**/*.zip
data/gpx/

# OS / editor
.DS_Store
**/.DS_Store
.idea/
.vscode/
.merge-backup/
.fit-tmp-*/

# Legacy monolith backup
index.monolith.bak
node_modules/
