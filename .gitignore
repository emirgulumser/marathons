# Garmin raw export (large, personal) — regenerate data/activities.json instead
DI_CONNECT/
garmin_data/
*.zip

# Generated personal user data — do not commit
data/activities.json
data/marathon-tracks.json
data/activity-details/
data/fits/
data/*.zip
data/*.fit

# Race-day weather JSON is app data (commit it). Raw FIT/GPX stays private.
data/gpx/

# OS / editor
.DS_Store
**/.DS_Store
.idea/
.vscode/
.merge-backup/
.fit-tmp-*/

# Legacy monolith backup
index.monolith.bak
node_modules/
